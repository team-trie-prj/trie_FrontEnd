# ── 원본 공공데이터 CSV (data.go.kr 다운로드, 대용량) ──
# preprocess_real.py 입력. 집계본(data/raw/*.csv)만 커밋한다.
/*.csv

# ── 비밀 키 (절대 커밋 금지) ──
.env
.env.local

# ── 생성 산출물 ──
rag.db
report_output.docx

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 에디터/OS/로컬 설정 ──
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
